import { Canvas, Meta } from '@storybook/blocks'

import * as Stories from './{{kebabCase (extractFilename path)}}.stories'

<Meta of={Stories} />

# {{pascalCase (extractFilename path)}}

{{pascalCase (extractFilename path)}} Component

<Canvas of={Stories.Default} />
